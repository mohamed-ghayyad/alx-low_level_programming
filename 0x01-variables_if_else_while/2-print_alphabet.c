#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - alphabet game a
 *
 * Return: 0 always.
 */

int main(void)
{
char alpha;
for (alpha = 'a'; alpha <= 'z'; alpha++)
putchar(alpha);
putchar('\n');
return (0);
}
